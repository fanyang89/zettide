---
- name: Benchmark a temporary Blob Pool over an existing Catalog Pool
  ansible.builtin.import_playbook: playbook.yml
  vars:
    zettide_test_profile_override: blob-pool-fio
    zettide_optimize_modes_override:
      - ReleaseSafe
