---
- name: Verify static FUSE CSI volumes with a temporary kind cluster
  ansible.builtin.import_playbook: playbook.yml
  vars:
    zettide_test_profile_override: csi-fuse-kind
    zettide_optimize_modes_override:
      - ReleaseSafe
