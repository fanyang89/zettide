---
- name: Benchmark a Catalog Volume through NVMe-oF/TCP and the Linux kernel initiator
  ansible.builtin.import_playbook: playbook.yml
  vars:
    zettide_test_profile_override: nvmf-catalog-fio
    zettide_optimize_modes_override:
      - ReleaseSafe
